<!DOCTYPE html>
<html>
<head>
  <title>My Best Tweets</title>
  <style type="text/css">
    body {
      width: 384px;
      padding: 0px;
      margin: 0px;
      font-family: Arial, sans-serif;
      font-size: 16px;
      line-height: 21px;
    }
    h1 {
      font-weight: bold;
      text-align: center;
      word-wrap: break-word;
      font-size: 20px;
      line-height: 28px;
      margin: 0 0 0.5em 0;
    }
    p {
      margin: 0 0 0.5em 0;
    }
    p.user {

    }
    img.avatar {
      float: left;
      margin-right: 10px;
    }
    img.logo {
      vertical-align: middle;
    }
    ol {
      border-top: 1px solid black;
      padding-left: 0px;
    }
    li {
      margin: 10px 0 0 0;
      border-bottom: 1px solid black;
      padding: 0 0 10px 0;
    }
    .position {
      display: inline-block;
      width: 15px;
      margin: 3px 5px 0 5px;
      vertical-align: top;
      font-weight: bold;
      font-size: 24px;
    }
    .content {
      display: inline-block;
      width: 350px;
      margin: 0 5px 0 0;
    }
    .about {
      font-size: 14px;
    }
    .about strong {
      font-weight: bold;
      font-size: 16px;
    }
    .cf:after {
          content:"";
          display:table;
          clear:both;
      }
  </style>
</head>
<body>
  <h1>My Best Tweets <img class="logo" src="<%= @domain %>/twitter_logo.png" width="30" height="25" /></h1>

  <p class="user cf">
    <img class="avatar" src="<%= @profile_image_url %>" width="48" height="48" />
    <strong><%= @user_name %></strong><br />
    @<%= @screen_name %>
  </p>

<p>
<%= @user_name %> sent <%= @total_tweets %> Tweet<% if @total_tweets > 1 %>s<% end %> in the past <% if @days_to_fetch == 1 %>24 hours<% else %><%= @days_to_fetch %> days<% end -%>
<% if @tweets.length < @total_tweets -%>
. The most popular <% if @tweets.length == 1 %>was<% else %>were<% end -%>
<% end -%>
:    
</p>

  <ol>
    <% @tweets.each_with_index do |t, n| %>
      <li>
        <span class="position"><%= n + 1 %></span>
        <span class="content">
          <%= t[:text] %><br />
          <span class="about">
            <% if t[:retweet_count] > 0 %>
              <strong><%= t[:retweet_count] %></strong> Retweet<% if t[:retweet_count] != 1 %>s<% end %><% if t[:favorite_count] > 0 %>,<% end %>
            <% end %>
            <% if t[:favorite_count] > 0 %>
              <strong><%= t[:favorite_count] %></strong> Favorite<% if t[:favorite_count] != 1 %>s<% end %>
            <% end %>
            <% if t[:retweet_count] && t[:favorite_count] %>
              ∙
            <% end %>
            <%= t[:created_at].strftime('%l:%M%P, %e %h %Y') %>
          </span>
        </span>
      </li>
    <% end %>
  </ol>
</body>
</html>